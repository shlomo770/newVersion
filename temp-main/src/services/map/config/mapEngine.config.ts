/** Engine-level defaults consumed by map services (not Redux UI state). */
export const INITIAL_STYLE_SERVICE_BASEMAP_ID = 'vector-global';

/** Style-change safety timeout — see `mapTimers.config` in features for full timer catalog. */
export const STYLE_CHANGE_SAFETY_TIMEOUT_MS = 10_000;
