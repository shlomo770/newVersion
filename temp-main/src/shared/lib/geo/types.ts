export type Coordinates = { lng: number; lat: number };

export interface Point {
  lat: number;
  lng: number;
}

export type LatLng = { lat: number; lng: number; alt: number };

export type LatLngManual = { lat: number; lng: number; alt: number; heading: number };
